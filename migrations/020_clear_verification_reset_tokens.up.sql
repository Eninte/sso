-- ============================================================================
-- T2 安全修复：reset/verification 令牌哈希存储（H2）
--
-- 背景：
--   - verification_tokens.token 与 reset_tokens.token 原存明文，
--     数据库泄露时可直接冒用邮箱验证/密码重置令牌
--   - 代码已切换为存储 SHA-256 hex（common.HashToken），校验时 hash 后比对
--   - 已签发的明文令牌与新比对逻辑不兼容，须全部清除
--
-- 影响：
--   - 在飞（已签发未使用）的验证/重置令牌全部失效
--   - 用户重新发起邮箱验证或密码重置流程即可，影响可接受
-- ============================================================================

DELETE FROM verification_tokens;
DELETE FROM reset_tokens;
