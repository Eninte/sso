-- ============================================================================
-- T1 安全修复：tokens 表去除明文存储（H1）— 回滚占位
--
-- 不可逆：明文数据已被清除，无法从 hash 恢复。
-- 回滚方式 = 代码回滚（恢复写入/读取明文），历史明文数据无法找回。
-- 保留空迁移占位以维持迁移版本链完整。
-- ============================================================================
SELECT 1;
